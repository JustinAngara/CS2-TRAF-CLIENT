#pragma once

/*
SETUP INSTRUCTIONS
	if you want to run anything before game tick 'safely'
	run here, this will run on the premise that minhook actually hooks onto game state
	don't screw it up now
*/
namespace Setup
{
	void Run()
	{

	}
}
